# ── Çekirdek Web/API ───────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
pydantic==2.7.4
pydantic-settings==2.3.4
SQLAlchemy==2.0.31
websockets==12.0

# ── Görüntü İşleme ─────────────────────────────────────────────────
opencv-python==4.10.0.84
numpy==1.26.4
Pillow==10.4.0

# ── Derin Öğrenme ───────────────────────────────────────────────────
# Eğitilmiş şiddet modeli (ResNet50+BiLSTM+Attention) ve Keypoint R-CNN
# için gereklidir. Varsayılan pip paketi CPU'da çalışır (yavaş ama tam
# işlevsel); NVIDIA GPU'da ~19x hız için kurulumdan SONRA şunu çalıştırın:
#   pip install torch==2.3.1 torchvision==0.18.1 --index-url https://download.pytorch.org/whl/cu121
torch==2.3.1
torchvision==0.18.1
